name: Publish

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Restore projects
        run: |
          dotnet restore fsrubik --ignore-failed-sources
          dotnet restore fsrubik.sharp --ignore-failed-sources

      - name: Build projects
        run: |
          dotnet build fsrubik --no-restore --configuration Release
          dotnet build fsrubik.sharp --no-restore --configuration Release

      - name: Pack projects
        run: |
          dotnet pack fsrubik --no-build --configuration Release --output ./nupkg
          dotnet pack fsrubik.sharp --no-build --configuration Release --output ./nupkg

      - name: Publish packs
        run: dotnet nuget push "nupkg/*.nupkg" --source "https://nuget.pkg.github.com/${{ github.repository_owner }}/index.json" --api-key "${{ secrets.GITHUB_TOKEN }}"
